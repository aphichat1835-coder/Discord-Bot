name: TEMP remove final work branch

on:
  pull_request:
    branches:
      - ttt.1

permissions:
  contents: write

jobs:
  cleanup:
    if: ${{ github.head_ref == 'cleanup/remove-final-work-branch-20260728' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Delete merged work branch and cleanup branch
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          branches=(
            "fix/require-isolated-gate-pr71"
            "cleanup/remove-final-work-branch-20260728"
          )
          for branch in "${branches[@]}"; do
            status=$(curl --silent --show-error \
              --output /tmp/delete-response.json \
              --write-out "%{http_code}" \
              --request DELETE \
              --header "Accept: application/vnd.github+json" \
              --header "Authorization: Bearer ${GH_TOKEN}" \
              --header "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/git/refs/heads/${branch}")
            case "${status}" in
              204) echo "Deleted ${branch}" ;;
              404) echo "Already absent: ${branch}" ;;
              422)
                if grep -q 'Reference does not exist' /tmp/delete-response.json; then
                  echo "Already absent: ${branch}"
                else
                  cat /tmp/delete-response.json
                  exit 1
                fi
                ;;
              *)
                cat /tmp/delete-response.json
                exit 1
                ;;
            esac
          done
