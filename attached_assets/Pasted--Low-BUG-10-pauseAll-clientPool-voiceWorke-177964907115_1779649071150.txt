🟡 Low — ข้อมูลไม่สมบูรณ์ / ปัญหาเล็กน้อย

BUG #10 — pauseAll() ไม่ล้าง clientPool

ไฟล์: voiceWorker.js บรรทัด 505-511

async function pauseAll() {
    isShuttingDown = true;
    stopAllNaturalTimers();
    for (const [id] of sessions) await sessionManager.pauseSession(id);
    // ← clientPool ไม่ถูกล้าง → selfbot clients ค้างอยู่ใน memory ตอน shutdown
}


BUG #11 — applyNaturalSettings() เปิด Timer สำหรับ Session ที่ยังไม่ Connect

ไฟล์: voiceWorker.js บรรทัด 659

for (const [sessionId] of sessionManager.getAllSessions()) {
    startNaturalTimer(sessionId); // ← รวม session ที่ client = null ด้วย
}
// doNaturalBlink() จะ return early แต่ timer ยังรันอยู่เรื่อยๆ


BUG #12 — member.joinedTimestamp อาจ null ใน AuditLogger

ไฟล์: auditLogger.js บรรทัด 101

`**เข้าร่วมเมื่อ:** <t:${Math.floor(member.joinedTimestamp / 1000)}:R>`
// Math.floor(null / 1000) = 0 → แสดง Jan 1, 1970


BUG #13 — Voice AuditLogger ขาด Event Unmute/Undeafen

ไฟล์: auditLogger.js Region 4

} else if (!oldState.serverMute && newState.serverMute) {
    title = `ถูก Server Mute`; // ✅ มี
// ⚠️ ขาด: oldState.serverMute && !newState.serverMute → คืนเสียง
// ⚠️ ขาด: oldState.serverDeaf && !newState.serverDeaf → คืน Deafen


BUG #14 — Dashboard โดน Rate Limit ตัวเอง

ไฟล์: index/server.js บรรทัด 27

// config: rateLimitRequests = 5 per 60s
// dashboard poll /api/status ทุก 5s = 12 requests/min → โดน 429
app.use('/api', createRateLimiter(requestCounts, config));


แก้ไข: เพิ่ม Whitelist สำหรับ /api/status หรือขึ้น limit เป็น 30-60 req/min

BUG #15 — /api/approve คืน 500 เมื่อ Guild อนุมัติซ้ำ

ไฟล์: index/server.js บรรทัด 215

await sessionManager.ApprovedGuildModel.create({ guildId });
// guildId เป็น unique → throw duplicate key error → 500 Internal Server Error
// ควรเป็น 409 Conflict หรือ upsert แทน


สรุปภาพรวม

|ระดับ      |จำนวน|รายการที่วิกฤตที่สุด                                            |
|----------|----|----------------------------------------------------------|
|🔴 Critical|4   |BUG #2 Crash healthCheck, BUG #1 shared client destroy    |
|🟠 Medium  |5   |BUG #5 DB query ทุก message, BUG #7 reconnectCount = 0 ตลอด|
|🟡 Low     |6   |BUG #13 audit ไม่สมบูรณ์, BUG #14 rate limit                 |


🔴 Critical Bugs

commands.js — handleModal

BUG #1 — Session สร้างในฐานข้อมูลแล้ว แต่ถ้า Voice เชื่อมต่อล้มเหลว ไม่ลบ Session ออก

// commands.js
const sessionId = await sessionManager.createSession(token, serverId, voiceId, ...);
await voiceWorker.startSession(sessionId, token); // ← ถ้า throw เช่น GUILD_NOT_FOUND
// ← ไม่มี cleanup! session ค้างใน DB และจะถูก autoResume() ดึงขึ้นมาทุกครั้งที่รีสตาร์ท


ผลลัพธ์: Ghost sessions สะสมใน MongoDB ทุกครั้งที่ใครกรอก voiceId ผิด session จะถูก retry ตลอด

แก้ไข:

try {
    await voiceWorker.startSession(sessionId, token);
} catch (err) {
    await sessionManager.deleteSession(sessionId); // cleanup ทันที
    throw err;
}


BUG #2 — serverName ถูกบันทึกเป็น Server ID ถ้าบอทไม่ได้อยู่ใน Guild นั้น

const targetGuild = client.guilds.cache.get(serverId);
const guildName = targetGuild ? targetGuild.name : serverId; // ← serverId เป็น "ชื่อ"!

await sessionManager.createSession(token, serverId, voiceId, guildName, ...);
// DB เก็บ serverName = "1463891557940854900" แทนที่จะเป็นชื่อจริง


ผลลัพธ์: DM แจ้งเตือน, Dashboard, Status Panel แสดงชื่อ Server เป็น ID ตัวเลข

commands/utility.js — handleRestore

BUG #3 — Channel ที่ Restore กลับมา ไม่ถูกใส่ใน Category (parentId หาย)

// utility.js — backup เก็บ parentId
channels: guild.channels.cache.map(c => ({
    name: c.name, type: c.type, parentId: c.parentId, // ← เก็บไว้
    permissionOverwrites: ...
}))

// แต่ตอน restore ไม่ได้ใช้ parentId เลย!
await guild.channels.create(cData.name, {
    type: cData.type,
    permissionOverwrites: mappedOverwrites,
    // parentId: ??? ← หายไปทั้งหมด
    reason: "Enterprise Restore"
});


ผลลัพธ์: ห้องทั้งหมดที่กู้คืนจะถูกสร้างนอก Category กระจัดกระจายไปทั่ว

แก้ไข: ต้อง restore Category ก่อน แล้ว map parentId ไปยัง ID ใหม่:

// ขั้น 1: restore GUILD_CATEGORY ก่อน สร้าง categoryIdMap
// ขั้น 2: restore ห้องอื่นๆ โดย parent: categoryIdMap.get(cData.parentId)


commands/moderation.js

BUG #4 — DM ส่งก่อน Action จริง ถ้า ban/kick/timeout ล้มเหลว User รับ DM เท็จ

// moderation.js
target.user.send({ embeds: [dmEmbed] }).catch(() => {}); // ← ส่ง DM ว่า "คุณถูกแบน" ก่อน
await target.ban({ reason });                             // ← ถ้า throw → ไม่ได้แบนจริง!


ผลลัพธ์: User ได้รับ DM ว่าถูก ban แต่ยังอยู่ใน server ได้ตามปกติ

แก้ไข: ย้าย .send(dmEmbed) ไปหลัง await target.ban() สำเร็จ

voiceWorker.js

BUG #5 — Pooled Client ถูก reuse โดยไม่ตรวจสอบว่ายังใช้งานได้อยู่

if (clientPool.has(tokenHash)) {
    session.client = clientPool.get(tokenHash); // ← reuse ทันที
    // ไม่เช็คว่า client ถูก destroy ไปแล้วหรือเปล่า
    // ไม่เช็คว่า client.isReady() หรือเปล่า
}


ผลลัพธ์: ถ้า client ถูก destroy ไประหว่างทาง (เช่นจาก BUG sessionManager เก่า) การ reuse จะให้ broken client ไปสร้าง voice connection และ throw โดยไม่มีคำอธิบาย

แก้ไข:

const existing = clientPool.get(tokenHash);
if (existing && existing.isReady?.()) {
    session.client = existing;
} else {
    clientPool.delete(tokenHash); // ล้างออกถ้า dead
    // login ใหม่...
}


index/server.js

BUG #6 — /api/approved/kick ออก Guild แต่ไม่หยุด Sessions — Session ค้างวิ่งต่อไปเรื่อยๆ

app.post("/api/approved/kick", ..., async (req, res) => {
    await guild.leave();                                    // ← บอทออก Guild
    await sessionManager.ApprovedGuildModel.deleteOne(...)  // ← ลบจาก approved
    // ← แต่ sessions ของ guild นี้ยังคงอยู่ใน Map และใน DB!
    // healthCheck() จะ throw GUILD_NOT_FOUND ทุก 90 วิตลอดไป
});


แก้ไข:

// หยุด session ทุกตัวของ guild นี้ก่อนออก
const sessions = Array.from(sessionManager.getAllSessions().values())
    .filter(s => s.serverId === guildId);
for (const s of sessions) await voiceWorker.stopSession(s.sessionId);
await guild.leave();

