

🟠 Medium Bugs

voiceWorker.js

BUG #7 — idleTimeoutMs cleanup ใช้ค่าจาก config.json ไม่ใช่จาก DB Setting

// voiceWorker.js
async function cleanupIdleSessions() {
    const maxIdle = config.limits.idleTimeoutMs; // ← hardcoded จาก config.json (86400000)


// server.js
app.post("/api/settings", ..., async (req, res) => {
    if (idleTimeoutHrs) await sessionManager.setSetting('idleTimeoutHrs', idleTimeoutHrs); // ← บันทึกลง DB


ผลลัพธ์: เปลี่ยนค่าผ่าน Dashboard ไม่มีผลจริง cleanup ยังใช้ค่า 24 ชั่วโมงตลอด

แก้ไข:

const savedHrs = await sessionManager.getSetting('idleTimeoutHrs', null);
const maxIdle = savedHrs ? savedHrs * 3600000 : config.limits.idleTimeoutMs;


BUG #8 — startBot() retry loop ไม่มีขีดจำกัด — วน infinite ถ้า TOKEN_MANAGER ผิด

// index.js
async function startBot() {
    try {
        await client.login(process.env.TOKEN_MANAGER);
    } catch (err) {
        setTimeout(startBot, 10000); // ← retry ทุก 10 วิตลอดไป ไม่มีวันหยุด
    }
}


แก้ไข: เพิ่ม retry counter หยุดหลัง N ครั้ง

BUG #9 — startRotateTimer() async ไม่มี lock — เรียกพร้อมกันได้ timer duplicate

let _rotateTimer = null;
async function startRotateTimer() {
    if (_rotateTimer) { clearInterval(_rotateTimer); _rotateTimer = null; }
    const s = await sessionManager.getAllSettings(); // ← รอ DB
    // ถ้ามีการเรียก 2 ครั้งพร้อมกัน ทั้ง 2 ผ่าน if (_rotateTimer) ซึ่งยังเป็น null
    _rotateTimer = setInterval(...); // ← ทั้ง 2 สร้าง timer ทำให้ rotate 2x เร็ว
}


commands/utility.js

BUG #10 — handleSay() — sayUsageTracking Map รั่วหน่วยความจำ (Empty Arrays ค้าง)

const sayUsageTracking = new Map(); // ← Module-level, ไม่เคยถูก cleanup ครบ

// หลัง 60 วิ history = [] (empty array)
sayUsageTracking.set(userId, []); // ← key ยังอยู่ใน Map ตลอด
// ทุก user ที่เคยพิมพ์ /say ครั้งแรกจะค้างเป็น entry ว่างตลอดชีวิต process


แก้ไข: ถ้า history.length === 0 ให้ sayUsageTracking.delete(userId) แทน

BUG #11 — handleSetupLog() ข้าม Channel ถ้า DB บอกว่ามีอยู่ แต่ไม่เช็คว่าจริงๆ ยังอยู่ใน Discord หรือเปล่า

const existing = await sessionManager.getLogChannelMap(interaction.guild.id);
if (existing && existing[key]) {
    created.push(`มีอยู่แล้ว`);
    continue; // ← ข้าม แต่ channel อาจถูกลบไปแล้ว!
}


ผลลัพธ์: ถ้า admin ลบ log channel แล้วรัน /setup-log อีกครั้ง — บอกว่า “มีอยู่แล้ว” แต่ audit log ไม่ทำงาน

แก้ไข:

if (existing && existing[key]) {
    const ch = interaction.guild.channels.cache.get(existing[key]);
    if (ch) { created.push(`มีอยู่แล้ว`); continue; }
    // ถ้าไม่เจอ → สร้างใหม่
}


BUG #12 — handleSteal() ไม่แยก animated vs regular emoji quota

const maxEmojis = interaction.guild.premiumTier === 2 ? 150 :
                  interaction.guild.premiumTier === 3 ? 250 : 100;
// ← นับรวม animated + static เป็น pool เดียว

const currentCount = emojiManager.cache.size; // ← รวม animated + static


ปัญหา: Discord แยก quota → 100 static + 100 animated (ไม่ใช่ 100 รวม) ถ้า Server เต็ม animated 100 ตัว แต่ static 0 — ระบบบอกว่าเต็มแล้ว 100/100 และปฏิเสธทุกอย่าง (แต่จริงๆ steal static ยังได้)

BUG #13 — handleRestoreConfirm() ทำงานใน Detached IIFE — ถ้าใช้เวลา >15 นาที interaction หมดอายุเงียบๆ

(async () => {
    try {
        // restore อาจใช้เวลา ~14 นาที
        await interaction.followUp({ ... }); // ← หลัง 15 นาที interaction.followUp ล้มเหลว
    } catch (err) {
        await interaction.followUp({ content: `เกิดข้อผิดพลาด...` }).catch(() => {}); // swallow
    }
})(); // ← not awaited — uncaught error จาก IIFE ไม่มีใครจัดการ


ผลลัพธ์: User เห็น “กำลังกู้คืน…” ตลอดไป ไม่มีผลลัพธ์

BUG #14 — handleWhitelist() ไม่ validate user_id ว่าเป็น Discord Snowflake จริงหรือเปล่า

const userId = interaction.options.getString("user_id");
if (action === "add") {
    await sessionManager.addWhitelist(userId, ...); // ← รับ string อะไรก็ได้


Admin สามารถเพิ่ม "hello", "../etc" หรือ string แปลกๆ เข้า Whitelist ได้

แก้ไข:

if (!/^\d{17,19}$/.test(userId)) {
    return interaction.reply({ content: `> User ID ต้องเป็นตัวเลข 17-19 หลัก`, ephemeral: true });
}


commands/moderation.js

BUG #15 — /ban ถูกบล็อคโดย Role Position Check ที่ไม่จำเป็น

if (target.roles.highest.position >= interaction.guild.members.me.roles.highest.position) {
    return interaction.reply({ content: `ยศของบอทต่ำกว่า...`, ephemeral: true });
}
// ← check นี้ใช้กับ kick/timeout เท่านั้น
// สำหรับ /ban: Discord อนุญาตให้ ban ใครก็ได้ที่มียศต่ำกว่า guild owner
// บอทสามารถ ban คนที่มียศสูงกว่าบอทได้ ถ้ามี BAN_MEMBERS permission


BUG #16 — status_stop_* button ไม่ตรวจสอบความเป็นเจ้าของ Session

// commands.js
if (customId.startsWith("status_stop_")) {
    const sId = customId.replace("status_stop_", "");
    await voiceWorker.stopSession(sId); // ← หยุด session โดยไม่เช็คว่า ownerId ตรงกันไหม


ถ้า customId รั่วออกไป ใครก็หยุด session ของคนอื่นได้

index/events.js

BUG #17 — Approval Gate ไม่ครอบคลุม Button Interactions หลังจากกด btn_status

// events.js
const isProtectedButton = interaction.isButton()
    && ["btn_start", "btn_status"].includes(interaction.customId);
// ← "status_page_*", "status_stop_*", "btn_stop_all" ไม่ถูกเช็ค
// Guild ที่ถูกเพิกถอน approval ยังจัดการ session ผ่าน button ได้


index/server.js

BUG #18 — /api/stop-session ใช้ checkRevealPin แทน checkAuth

app.post("/api/stop-session", express.json(), async (req, res) => {
    if (!checkRevealPin(req, res)) return; // ← ใช้ WEB PIN ไม่ใช่ API_SECRET


ปัญหา: ใครที่รู้ Shadow Portal PIN (ซึ่งเริ่มต้นคือ 123456) สามารถหยุด session ของ user ทุกคนได้โดยไม่ต้องรู้ API_SECRET

auditLogger.js

BUG #19 — messageDelete Partial Message → embed แสดง <@undefined>

client.on("messageDelete", async (message) => {
    if (!message.guild || message.author?.bot) return;
    // ↑ ถ้า message.author เป็น null (partial) → !null.bot → TypeError
    // หรือ message.author?.bot → undefined → !undefined → true → ผ่านไปทำงานต่อ

    `**ผู้ส่ง:** <@${message.author?.id}>` // ← แสดง "<@undefined>"


แก้ไข: เพิ่ม if (message.partial || !message.author) return;

BUG #20 — messageUpdate Partial Old Message → ข้ามทั้งที่ข้อความมีการเปลี่ยนแปลงจริง

client.on("messageUpdate", async (oldMsg, newMsg) => {
    if (!oldMsg.content && !newMsg.content) return; // ← !null = true → return early!
    // oldMsg.content เป็น null สำหรับ partial message
    // แม้ newMsg.content จะมีข้อความ ก็ถูก return ทิ้ง


🟡 Low / Design Issues

BUG #21 — information.js — botCount ใช้ Cache ไม่ครบ อาจผิดพลาด

// information.js
const botCount = guild.members.cache.filter(m => m.user.bot).size;
// guild.members.cache ไม่ได้มีสมาชิกทั้งหมด (ขึ้นกับ cache limit)
const humanCount = guild.memberCount - botCount; // ← humanCount ผิด


BUG #22 — information.js — member.joinedTimestamp อาจ null → แสดง Jan 1, 1970

`<t:${Math.floor(member.joinedTimestamp / 1000)}:R>` // Math.floor(null/1000) = 0


BUG #23 — information.js — /ping latency อาจติดลบ

const latency = sent.createdTimestamp - interaction.createdTimestamp;
// ถ้า clock skew → latency < 0 → แสดงผลแปลก
// แก้: Math.max(0, sent.createdTimestamp - interaction.createdTimestamp)


BUG #24 — commands.js — Panel Status embed แสดงสถานะ “กำลังเชื่อมต่อ” เสมอ ไม่ตรวจสอบจริง

`— **สถานะ:** ${config.emojis.status_online} กำลังเชื่อมต่อ\\n`
// ← hardcoded เสมอ แม้ connection จะ Disconnected หรือ Destroyed จริงๆ


ควรตรวจสอบ session.connection?.state?.status

BUG #25 — auditLogger.js — guildMemberRemove - member.user.tag crash สำหรับ partial member

`\`${member.user.tag}\``
// member.user อาจ null ใน certain edge cases
// แก้: member.user?.tag || 'Unknown'


BUG #26 — utility.js — Backup snapshotId ไม่ unique ต่อ guildId ใน Schema ทำให้หลาย backup ต่อ guild ได้

// sessionManager.js Schema
const snapshotSchema = new mongoose.Schema({
    snapshotId: { type: String, unique: true }, // ← unique ต่อ snapshotId
    guildId: String,  // ← ไม่มี unique constraint!


Race condition ในทีมที่ทำ backup พร้อมกัน หรือ bug ทำให้มี backup หลาย document ต่อ guildId

BUG #27 — index/server.js — /health และ /ping ไม่ผ่าน Rate Limiter เลย

app.use('/api', createRateLimiter(...)); // ← เฉพาะ /api/*
app.get("/health", ...); // ← ไม่มี rate limit
app.get("/ping", ...);   // ← ไม่มี rate limit
// ← สามารถ flood ได้


BUG #28 — index/system.js — CRON 90s อาจ overlap ถ้า healthCheck() ใช้เวลา >90 วินาที

setInterval(async () => {
    await voiceWorker.cleanupIdleSessions();
    await voiceWorker.healthCheck(); // ← อาจใช้เวลานานถ้า session เยอะ
    await sessionManager.saveDatabase();
}, 90000); // ← ถ้างานก่อนยังไม่เสร็จ งานใหม่ก็เริ่มแล้ว


ควรใช้ pattern let running = false; if (running) return; running = true; try {...} finally { running = false; }

BUG #29 — index/server.js — wh.destroy() เรียกก่อน wh.send() resolve (ทั่วทั้ง codebase)

// พบรูปแบบนี้ใน events.js, index.js, server.js, utility.js
const wh = new WebhookClient({ url: ... });
wh.send({ content: `...` }).catch(() => {}); // ← async ยังไม่เสร็จ
wh.destroy();                                 // ← destroy ทันที อาจตัด request กลางคัน


แก้ไข: await wh.send({...}).catch(() => {}); wh.destroy();

BUG #30 — commands/moderation.js — handleVoiceKickAll() ใช้ memberSnapshot เก่า แต่เช็ค vc.members จาก snapshot ที่อาจ stale สำหรับ kick status

ไม่ใช่ crash แต่ถ้าสมาชิกออกระหว่าง loop → member.voice.disconnect() อาจ throw silently

BUG #31 — sessionManager.js — saveDatabase() ไม่อัปเดต startedAt — restart ทำให้ uptime ใน DM ผิด

ops.push({
    updateOne: {
        filter: { sessionId: id },
        update: { $set: {
            token: session.token, serverId: ..., lastActivity: ...,
            // ← ไม่ได้ update startedAt ซึ่งถูกเซ็ตตอน createSession
        }}
    }
});


เมื่อ restart, loadDatabase() โหลด startedAt เดิมจาก DB มาถูกต้อง แต่ session.reconnectCount ถูก reset เป็น 0 ทุกครั้งที่ restart

BUG #32 — auditLogger.js — registerMemberEvents และ registerVoiceEvents require config.json ซ้ำทุกครั้งที่ register

function registerMessageEvents(client, sessionManager) {
    const config = require("../config.json"); // ← inside function

function registerMemberEvents(client, sessionManager) {
    const config = require("../config.json"); // ← ซ้ำอีก

function registerVoiceEvents(client, sessionManager) {
    const config = require("../config.json"); // ← ซ้ำอีก


Node.js cache require → ไม่มีผลต่อ performance แต่ควรย้ายขึ้น top-level

BUG #33 — index/server.js — Dashboard polling /api/status โดนตัวเอง Rate Limit (5 req/60s)

// config.json
"rateLimitRequests": 5,
"rateLimitWindowMs": 60000
// Dashboard ถ้า poll ทุก 5 วิ = 12 req/min → โดน 429 หลังวินาทีที่ 25


BUG #34 — index.js — checkApproval() ส่ง Webhook แบบ fire-and-forget ทุกครั้งที่มี unauthorized command แม้จะเป็น command เดิมจาก user เดิม — Webhook spam

ไม่มี debounce เหมือนกับ anti-raid debounce ควรเพิ่ม cooldown ต่อ ${guild.id}\_${user.id}

BUG #35 — commands/utility.js — handleBackup() บันทึก permissions.bitfield เป็น string แต่ restore ใช้ BigInt(rData.permissions || "0")

// backup
permissions: r.permissions.bitfield.toString() // ← toString() ของ BigInt

// restore
permissions: BigInt(rData.permissions || "0")  // ← ถ้า rData.permissions เป็น "" → BigInt("0") ✓
                                                // ถ้า rData.permissions เป็น undefined → BigInt("0") ✓
                                                // ถ้า rData.permissions เป็น "0n" → BigInt("0n") → SyntaxError!


ใน Discord.js v13 bitfield.toString() อาจคืน "0n" สำหรับบาง Role ควรใช้ .toString(10) หรือ String(Number(r.permissions.bitfield))

BUG #36 — index/events.js — guildCreate สร้าง Invite ที่ไม่จำเป็น (maxAge: 0 = ตลอดไป) และส่งเข้า Webhook โดยไม่มีการยืนยัน

const inv = await channel.createInvite({ maxAge: 0 }); // ← invite ไม่หมดอายุ
inviteStr = inv.url; // ← ส่งเข้า Webhook ทันที


Invite ไม่หมดอายุ และถูกสร้างในทุก Guild ที่บอทเข้า ควรใช้ maxAge: 3600 (1 ชั่วโมง)

BUG #37 — sessionManager.js — reconnectTracking Map ไม่ถูกใช้งานจริง แต่ยังถูก export

// sessionManager.js
const reconnectTracking = new Map(); // ← ใช้ใน addReconnect()
function addReconnect(sessionId) { ... } // ← ไม่เคยถูกเรียกจากที่ไหน

module.exports = {
    addReconnect, clearReconnect // ← export แต่ไม่มี caller


Dead code ใน export — สับสนกว่าเดิม ควรเอาออกหรือใช้งานจริง

BUG #38 — information.js — /help แสดง /panel เฉพาะถ้า isAdmin แต่ command อื่นๆ ก็ต้องการ ADMINISTRATOR เหมือนกัน — ข้อมูล help ไม่ตรงกับ permission จริง

if (isAdmin)
    `— /panel — เรียกแผงควบคุม (ซ่อนจากผู้ใช้ทั่วไป)`
// แต่ /backup, /restore, /setup-log ก็ต้องการสิทธิ์สูง
// help text ไม่แสดง requirement เหล่านี้ชัดเจน


📊 สรุปรวม

|ไฟล์                           |Bugs|ที่วิกฤตที่สุด                                          |
|------------------------------|----|--------------------------------------------------|
|`voiceWorker.js`              |6   |Client pool reuse โดยไม่เช็ค, healthCheck null crash|
|`commands.js` + `handleModal` |5   |Ghost session ไม่ถูกลบ, serverName เป็น ID           |
|`utility.js`                  |7   |parentId หายตอน restore, IIFE >15min ล้มเหลว       |
|`moderation.js`               |4   |DM ก่อน action, ban role check ผิด                  |
|`information.js`              |4   |joinedTimestamp null, botCount cache              |
|`server.js`                   |5   |kick guild ไม่ stop sessions, stop-session auth ต่ำ  |
|`events.js` + `auditLogger.js`|5   |Partial message crash, approval gate gap          |
|`sessionManager.js`           |3   |addReconnect dead code, reconnectCount = 0        |
|`index.js` + `system.js`      |4   |startBot infinite retry, rotateTimer race         |


