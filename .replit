modules = ["nodejs-24"]
run = "cd discord && node index.js"

[agent]
stack = "PNPM_WORKSPACE"
expertMode = true

[[ports]]
localPort = 3000
externalPort = 80

[deployment]
deploymentTarget = "cloudrun"
run = ["sh", "-c", "cd discord && node index.js"]
